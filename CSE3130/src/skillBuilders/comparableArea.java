package skillBuilders;

public class comparableArea extends Rectangle
{
	/*
	 * Determines if object r is smaller, the same, or larger
	 * than this rectangle object
	 * pre r is a rectangle object
	 * post -1 has been returned if object r is larger than this rectangle
	 * 0 has been returned if they are the same
	 * 1 has been returned if r is smaller than the rectangle 
	 * 
	 */

}
